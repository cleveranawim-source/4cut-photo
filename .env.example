# 서버가 외부에 공개되는 정확한 HTTPS 주소입니다. 끝의 /는 빼주세요.
# 비워두면 접속한 주소를 자동 사용합니다.
PUBLIC_BASE_URL=https://fourcut.example.com

# QR 사진 보관 시간(분). 최소 5분입니다.
SHARE_TTL_MINUTES=60

# 운영 서버 포트
PORT=4173

# Railway Volume을 /data에 연결할 때 사용합니다.
STORAGE_DIR=/data
